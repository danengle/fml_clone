<% form_for([:admin,@category]) do |f| %>
  <%= f.error_messages %>
  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
