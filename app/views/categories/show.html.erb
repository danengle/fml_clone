<% if @posts.blank? %>
  <div class="boxed">
    <p>There doesn't seem to be anything here...</p>
  </div>
<% else %>
  <%= render @posts %>
  <div id="paginate">
    <%= will_paginate @posts %>
    <div class="page_entries">
      <%= page_entries_info @posts %>
    </div>
  </div>
<% end %>